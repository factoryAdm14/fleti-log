name: shared_flutter
description: Camada compartilhada Fleti — services, models, widgets e theme para apps web/mobile.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.7
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.3
  geolocator: ^13.0.2
  dart_pusher_channels: ^1.3.1
  google_maps_flutter: ^2.10.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
