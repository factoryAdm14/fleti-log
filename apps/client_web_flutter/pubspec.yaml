name: client_web_flutter
description: Fleti — App Web do Cliente
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  provider: ^6.1.2
  google_maps_flutter: ^2.10.0
  url_launcher: ^6.3.1
  shared_flutter:
    path: ../../shared_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
